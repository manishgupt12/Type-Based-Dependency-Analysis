ReadMe.txt

The run command requires to give directory path and pattern search. If no arguments are given for xml path save it will save those in current Directory.

Default path of xml is saved in debug directory.